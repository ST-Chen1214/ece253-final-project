# Pretrained Models / External Dependency (UDCUNet)

This project uses **UDCUNet** as an external dependency.
To avoid Git submodule issues, the UDCUNet source code is NOT included in this repository.

---

## 1. Download UDCUNet

Please clone the official UDCUNet repository:

```bash
git clone https://github.com/J-FHu/UDCUNet.git external/UDCUNet